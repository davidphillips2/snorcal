{
  "name": "snorcal",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "pnpm --parallel --filter backend --filter frontend run dev",
    "build": "pnpm --filter shared build && pnpm --parallel --filter backend --filter frontend run build",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
